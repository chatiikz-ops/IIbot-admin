const API_URL=process.env.NEXT_PUBLIC_API_URL||"http://localhost:3000";const DEFAULT_TIMEOUT=15000;let accessToken:string|null=null;let refreshPromise:Promise<boolean>|null=null;export type AuthEvent="changed"|"session-expired";const listeners=new Set<(event:AuthEvent)=>void>();
export function setAccessToken(token:string|null,event:AuthEvent="changed"){accessToken=token;listeners.forEach(fn=>fn(event))}export function getAccessToken(){return accessToken}export function subscribeAuth(fn:(event:AuthEvent)=>void){listeners.add(fn);return()=>{listeners.delete(fn)}}
export class ApiError extends Error{constructor(message:string,public status:number,public details?:unknown){super(message);this.name="ApiError"}}
type QueryValue=string|number|boolean|null|undefined;type Options={query?:Record<string,QueryValue>;body?:unknown;signal?:AbortSignal;timeout?:number};
async function request<T>(method:string,path:string,options:Options={},retry=true):Promise<T>{const url=new URL(path,API_URL.endsWith("/")?API_URL:`${API_URL}/`);Object.entries(options.query||{}).forEach(([k,v])=>{if(v!==undefined&&v!==null&&v!=="")url.searchParams.set(k,String(v))});const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),options.timeout??DEFAULT_TIMEOUT),abort=()=>controller.abort();options.signal?.addEventListener("abort",abort,{once:true});const form=options.body instanceof FormData;try{const response=await fetch(url,{method,headers:{...(options.body&&!form?{"Content-Type":"application/json"}:{}),...(accessToken?{Authorization:`Bearer ${accessToken}`}:{})},body:options.body===undefined?undefined:form?options.body as FormData:JSON.stringify(options.body),signal:controller.signal,cache:"no-store",credentials:"include"});if(response.status===204)return undefined as T;const text=await response.text();let payload:unknown;try{payload=text?JSON.parse(text):undefined}catch{payload=text}if(response.status===401&&retry&&!path.startsWith("/auth/")){if(await refreshAccessToken())return request<T>(method,path,options,false);setAccessToken(null,"session-expired");throw new ApiError("Сессия завершена. Войдите снова.",401)}if(!response.ok){const data=payload as{message?:string|string[]}|undefined;const backend=Array.isArray(data?.message)?data.message.join(". "):data?.message;const fallback=response.status===403?"Недостаточно прав для этого действия":response.status===409?"Операция недоступна в текущем состоянии":`Не удалось выполнить запрос`;throw new ApiError(backend||fallback,response.status,payload)}return payload as T}catch(error){if(error instanceof ApiError)throw error;if(controller.signal.aborted)throw new ApiError("Сервер не ответил вовремя",0,error);throw new ApiError("Нет связи с сервером",0,error)}finally{clearTimeout(timer);options.signal?.removeEventListener("abort",abort)}}
export async function refreshAccessToken(){if(refreshPromise)return refreshPromise;refreshPromise=(async()=>{try{const response=await fetch(new URL("/auth/refresh",API_URL.endsWith("/")?API_URL:`${API_URL}/`),{method:"POST",credentials:"include"});if(!response.ok)throw new Error();const data=await response.json()as{accessToken:string};setAccessToken(data.accessToken);return true}catch{setAccessToken(null);return false}finally{refreshPromise=null}})();return refreshPromise}
export const api={get:<T>(path:string,options?:Omit<Options,"body">)=>request<T>("GET",path,options),post:<T>(path:string,body?:unknown,options?:Omit<Options,"body">)=>request<T>("POST",path,{...options,body}),patch:<T>(path:string,body?:unknown,options?:Omit<Options,"body">)=>request<T>("PATCH",path,{...options,body}),delete:<T>(path:string,options?:Omit<Options,"body">)=>request<T>("DELETE",path,options)};
export function unpackList<T>(payload:unknown):{items:T[];total:number}{if(Array.isArray(payload))return{items:payload,total:payload.length};const value=(payload||{})as Record<string,unknown>,items=(value.items||value.data||value.results||[])as T[],meta=(value.meta||{})as Record<string,unknown>;return{items:Array.isArray(items)?items:[],total:Number(value.total??meta.total??items.length??0)}}export{API_URL};
